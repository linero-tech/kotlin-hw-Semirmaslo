package lms_126

fun task9(): String {

    return """  
        INPUT CC to machine
        INPUT CC code
        INPUT amount to withdraw
        IF amount to withdraw > users balance
        OUTPUT request new amount
        ELSE output money to user
        Update the users account
        Ask if the CC user wants payslip if (YES) = print payslip
        OUTPUT card
    """
}

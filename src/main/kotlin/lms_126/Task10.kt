package lms_126

fun task10(): String {

    return """
        INPUT email
        INPUT password
        IF email is wrong
        OUTPUT email error
        ELSE if password is less then 6 chars long
        OUTPUT password error
        ELSE create account
    """
}
